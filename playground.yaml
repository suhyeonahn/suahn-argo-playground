apiVersion: v1
kind: Pod
metadata:
  name: wil-playground
  namespace: dev
spec:
  containers:
    - name: wil-playground
      image: wil42/playground:v1
  tolerations:
    - key: node.kubernetes.io/disk-pressure
      operator: Exists
      effect: NoSchedule
